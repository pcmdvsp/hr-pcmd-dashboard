import { useState } from 'react'
import { supabase } from '../lib/supabaseClient'

export default function ResetPasswordDialog({ employees, onClose }) {
  const [email, setEmail] = useState(''); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [sending, setSending] = useState(false)
  const submit = async event => { event.preventDefault(); const normalizedEmail = email.trim().toLowerCase(); const employee = employees.find(item => item.email.trim().toLowerCase() === normalizedEmail); if (!employee) return setError('No active employee was found with this email address.'); if (employee.role !== 'normal') return setError('This feature is available only for normal user accounts.'); setSending(true); setError(''); setSuccess(''); const flag = await supabase.from('profiles').update({ must_change_password: true }).eq('id', employee.id); if (flag.error) { setSending(false); return setError(flag.error.message) }; const redirectTo = `${window.location.origin}${window.location.pathname}`; const result = await supabase.auth.resetPasswordForEmail(normalizedEmail, { redirectTo }); setSending(false); if (result.error) { await supabase.from('profiles').update({ must_change_password: employee.must_change_password }).eq('id', employee.id); return setError(result.error.message) }; setSuccess(`A password reset link has been sent to ${normalizedEmail}.`) }
  return <div className="modal-backdrop" role="presentation"><div className="modal"><form className="status-form reset-password-form" onSubmit={submit}><div className="form-title"><div><p className="eyebrow">ACCOUNT ADMINISTRATION</p><h2>Send password reset link</h2></div><button type="button" className="close" onClick={onClose} aria-label="Close">×</button></div><p className="subtle">The user will receive a link by email and must set a new password before using the system.</p><label>Employee email<input required type="email" value={email} onChange={event => setEmail(event.target.value)} placeholder="name@company.com" autoFocus /></label>{error && <p className="form-error">{error}</p>}{success && <p className="form-success">{success}</p>}<button className="primary-button" disabled={sending || Boolean(success)}>{sending ? 'Sending...' : success ? 'Link sent' : 'Send password reset link'}</button></form></div></div>
}
